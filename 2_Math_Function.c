#include "monty.h"

/**
 * _sub - Subtracts the top element from the second element of the stack.
 * @h: Pointer to the top node of the stack.
 * @n: Line number of the opcode.
 */
void _sub(stack_t **h, unsigned int n)
{
	(void)h;
	(void)n;
}
/**
 * _mod - Computes the modulus of the second element by
 * the top element of the stack.
 * @h: Pointer to the top node of the stack.
 * @n: Line number of the opcode.
 */
void _mod(stack_t **h, unsigned int n)
{
	(void)h;
	(void)n;
}
